# Core365-gpo-duplicate-settings-analyzer
Detect duplicate and conflicting GPO settings in Active Directory with a PowerShell script that generates an interactive HTML dashboard for analysis and reporting.
